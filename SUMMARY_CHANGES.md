# Сводка: Система проверки изменений

## ✅ Что реализовано

### 1. Проверка изменений за последние 24 часа (Рекомендуется)

**Команда:** `npm run check:recent`

**Что делает:**
- ✅ Парсит HTML страницы изменений каждого филиала
- ✅ Извлекает время изменений в формате "17-09-2025 · 02:05"
- ✅ Сравнивает с текущим временем (последние 24 часа)
- 💾 **Сохраняет результат в `branches.json` ПОТОКОВО** (после проверки каждого филиала)
- 🔥 Показывает список филиалов с изменениями

**Добавленные поля в `branches.json`:**
```json
{
  "hasRecentChanges": true,        // есть ли изменения за 24ч
  "recentChangesCount": 2,         // количество изменений за 24ч
  "lastChangeTime": "17-09-2025 · 02:05"  // время последнего изменения
}
```

**Преимущества:**
- ⚡ Быстро (проверяет только наличие, не парсит детали)
- 💾 Надёжно (сохраняет после каждого филиала)
- 🎯 Целевая проверка (только за 24 часа)

---

### 2. Полная история изменений

**Команда:** `npm run fetch:changes`

**Что делает:**
- 📜 Собирает ВСЮ историю изменений каждого филиала
- 📊 Парсит детали: название изменения, старое/новое значение, время, автор
- 💾 Сохраняет в отдельный файл `data/branches-changes.json`

**Структура данных:**
```json
{
  "branchId": "1484809282",
  "changesUrl": "https://yandex.ru/sprav/...",
  "totalChanges": 23,
  "changes": [
    {
      "title": "Изменение графика работы",
      "oldValue": "круглосуточно",
      "newValue": "ежедневно 9:00-22:00",
      "timestamp": "17-09-2025 · 02:05",
      "author": "ya.robot"
    }
  ]
}
```

---

## 📝 Примеры использования

### Быстрая проверка (ежедневный мониторинг)
```bash
# 1. Обновить список филиалов
npm run run:once

# 2. Проверить изменения за 24ч
npm run check:recent

# 3. Отфильтровать филиалы с изменениями
# (можно читать branches.json и фильтровать по hasRecentChanges: true)
```

### Детальный анализ
```bash
# Собрать полную историю
npm run fetch:changes

# Результат в data/branches-changes.json
```

---

## 🔧 Технические детали

### Парсинг времени
Формат Яндекса: `"17-09-2025 · 02:05"` (день-месяц-год · час:минута)

Регулярное выражение:
```javascript
/(\d{2})-(\d{2})-(\d{4})\s*·\s*(\d{2}):(\d{2})/
```

### Обработка HTML
Селекторы:
- `.RequestChanges.CompanyChangesPage-Request` - блок изменений
- `.RequestChanges-RequestTime` - время изменения
- `.CompanyChanges-ChangeTitle` - название изменения
- `.CompanyChanges-ChangeDiffItem_action_add` - новое значение
- `.CompanyChanges-ChangeDiffItem_action_remove` - старое значение

### Параллельная обработка
- По умолчанию: 3 одновременных запроса
- Настраивается в `src/check-recent-changes.ts`: `const limit = pLimit(3);`

---

## 📊 Производительность

| Задача | Время (185 филиалов) | Результат |
|--------|----------------------|-----------|
| Проверка за 24ч | ~10-15 мин | Обновлённый branches.json |
| Полная история | ~20-30 мин | Новый branches-changes.json |

---

## 🚀 Автоматизация

### Ежедневная проверка (cron)
```bash
# Каждый день в 9:00
0 9 * * * cd /path/to/project && npm run check:recent
```

### Интеграция с Telegram
Можно добавить отправку уведомлений о филиалах с изменениями:
```typescript
import { checkAllRecentChanges } from './src/check-recent-changes.js';
import { sendTelegramMessage } from './src/telegram.js';

// После проверки
const branchesWithChanges = branches.filter(b => b.hasRecentChanges);
if (branchesWithChanges.length > 0) {
  await sendTelegramMessage(`🔥 Найдены изменения в ${branchesWithChanges.length} филиалах!`);
}
```

---

## 📁 Структура файлов

```
data/
├── branches.json              # Основной файл с флагами изменений
└── branches-changes.json      # Полная история (опционально)

src/
├── check-recent-changes.ts    # Проверка за 24ч (быстро, потоково)
├── fetch-changes.ts           # Полная история (медленно, детально)
└── yandex.ts                  # Функции парсинга
```

---

## ⚠️ Важные замечания

1. **Потоковое сохранение** - `branches.json` обновляется после каждого филиала, поэтому даже при прерывании уже проверенные филиалы сохранены

2. **Часовой пояс** - парсинг использует локальное время браузера (Москва)

3. **Капча** - при большом количестве запросов может появиться SmartCaptcha. Решение: уменьшить `limit` или использовать `BROWSER_HEADLESS=false`

4. **Авторизация** - требуется активная сессия Яндекса (файл `data/auth-state.json`)

---

## 📖 Дополнительная документация

- [QUICKSTART_CHANGES.md](QUICKSTART_CHANGES.md) - Быстрый старт
- [CHANGES_PARSING.md](CHANGES_PARSING.md) - Детальное описание парсинга
- [README.md](README.md) - Общая документация проекта

